<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>상품 목록</title>
    <link rel="stylesheet" href="/stylesheets/product.css">
</head>

<body>
    <div class="title-bar">
        <h1>상품 목록</h1>
        <a href="/dashboard">메인으로 돌아가기</a>
    </div>

    <% if (products && products.length> 0) { %>
        <table border="1" cellpadding="10" cellspacing="0">
            <thead>
                <tr>
                    <th>상품 ID</th>
                    <th>상품명</th>
                    <th>가격</th>
                    <th>등록일</th>
                    <th>상세보기</th>
                </tr>
            </thead>
            <tbody>
                <% products.forEach(product=> { %>
                    <tr>
                        <td>
                            <%= product.product_id %>
                        </td>
                        <td>
                            <%= product.name %>
                        </td>
                        <td>
                            <%= product.price %>원
                        </td>
                        <td>
                            <%= product.created_at %>
                        </td>
                        <td><a href="/products/<%= product.product_id %>">자세히 보기</a></td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
        <% } else { %>
            <p>등록된 상품이 없습니다.</p>
            <% } %>
</body>

</html>